{
  "name": "olympus",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "lint": "tsc --project tsconfig.json --noEmit",
    "build": "npm run lint && vite build"
  },
  "devDependencies": {
    "vite": "^7.1.5"
  }
}
